# AMR Simulation — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages
# Or in venv: pip install -r requirements.txt

# ── Core simulation ────────────────────────────────────────────────────────
mesa>=2.1.0,<3.0.0           # Agent-based modeling framework
numpy>=1.24.0                # Grid operations, array math
scipy>=1.10.0                # Scientific computing (diffusion, stats)

# ── API server ─────────────────────────────────────────────────────────────
fastapi>=0.100.0             # REST + WebSocket API
uvicorn[standard]>=0.22.0    # ASGI server
pydantic>=2.0.0              # Request validation
websockets>=11.0             # WebSocket support

# ── Data & analytics ───────────────────────────────────────────────────────
requests>=2.31.0             # HTTP client (for CARD API calls)

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0

# ── Optional: future ML resistance prediction ──────────────────────────────
# torch>=2.0.0              # PyTorch — for GNN resistance gene predictor
# torch-geometric>=2.3.0    # Graph Neural Networks on genomic data
# scikit-learn>=1.3.0       # Baseline ML models
# pandas>=2.0.0             # Data manipulation
# biopython>=1.81           # Biological sequence analysis